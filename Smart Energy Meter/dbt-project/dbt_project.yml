name: 'smart_energy_meter'
version: '1.0.0'
config-version: 2

profile: 'smart_energy_meter'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  smart_energy_meter:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
